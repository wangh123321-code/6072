version: '3.8'

services:
  cat-health-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cat-health-dashboard
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
